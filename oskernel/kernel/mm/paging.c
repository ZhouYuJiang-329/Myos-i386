#include "paging.h"

// 完善页表（在head.asm设置的基础上添加更多映射）
void paging_init(void) {
    // 页目录和页表已在head.asm中设置好基本的恒等映射（低1MB）
    // 这里可以添加更多映射，例如：
    // - 高地址内存映射
    // - 内核高地址映射
    // - 用户空间映射

    // 目前只保留基本映射，后续可以扩展
}
